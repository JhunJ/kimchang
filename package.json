{
  "name": "kimchang",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:pages": "VITE_BASE_PATH=/kimchang/ npm run build",
    "preview": "vite preview",
    "preview:pages": "VITE_BASE_PATH=/kimchang/ npm run build && vite preview --base /kimchang/"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.5"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3",
    "vite": "^6.2.5"
  }
}
